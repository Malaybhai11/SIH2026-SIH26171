{
  "$comment": "Machine copy of the /agent/synthesize RESPONSE schema (docs/api-contract.md). Used to constrain LLM structured output and for server-side validation.",
  "type": "object",
  "properties": {
    "answer": { "type": "string" }
  },
  "required": ["answer"],
  "additionalProperties": false
}
